# OROCHI System 改修 TODOリスト

## 🤖 Claude Codeへの指示
あなたはOROCHI Systemの改修を担当するシニアフロントエンドエンジニアです。
このファイルは、今回の改修要件をまとめたチェックリストです。
以下のルールに従って作業を進めてください。

1. **ステップバイステップで実行**: 
   一度にすべての要件を実装するのではなく、要件を1つずつ（またはサブタスクごとに）実装してください。
2. **完了ごとのファイル更新**: 
   サブタスクまたは要件が1つ完了するごとに、この `todo.md` ファイルの `[ ]` を `[x]` に書き換え、ファイルを保存してください。
3. **人間への確認**: 
   1つの大項目（要件1、要件2など）が完了し、テストが済んだら、次の項目に進む前に人間にプレビューやレビューを求めてください。
4. **設計の参照**:
   システム全体像やデータ構造については、同ディレクトリにある `space.md` を適宜参照してください。

---

## 📋 タスクリスト

### [x] 要件1: クレジットカード手順（Task No.3）の図解入りマニュアル実装と視認性向上
- [x] `app/dashboard/detail/page.tsx` 内の `INITIAL_TASKS` の `no: "3"` の `manual` (HTML文字列) を刷新する。
- [x] テキストは `credit_card_manual.md` を使用すること。このMarkdownファイル内には、HTMLの `<div>` や `flex` を用いた「残高ズレの図解」や「ステップボックス」が既に定義されているので、そのHTML構造を崩さず、Tailwind CSS等と競合しないように Reactコンポーネント内の文字列として正確にマージすること。
- [x] `app/globals.css` の `.manual-content` 周りを調整し、ダークモードの画面内でも手順書（白・パステル背景）が浮き上がってハッキリと読みやすくなるよう視認性を向上させる。

### [x] 要件2: ECオロチ売上入力欄の「複数アカウント（多店舗）展開」対応
- [x] 売上データ入力テーブル（`task.type === 'sales_input'`）に、「店舗を追加」できるUI（ボタン等）を実装する。
- [x] 店舗名（例：楽天市場、Yahoo!等）のテキストを、ユーザーがインラインで自由に編集できる仕様にする。
- [x] `handleOrochiDataChange` などのFirestore保存・更新ロジックを、固定配列（`SHOPS`）依存から動的なキー・オブジェクト配列に対応できるよう修正する。

### [x] 要件3: ECオロチ以外の「その他事業」の枠追加と「総集計」の実装
- [x] 入力エリアに、ECオロチとは独立した「その他の事業」の入力枠（テーブル等）を最低2枠作成する。
- [x] その他事業の「タイトル（事業名）」をユーザーが編集・保存できるようにする。
- [x] `[ECオロチ合算] + [その他事業合算]` = 「総売上高」および「総仕入高」を算出する計算ロジックを実装する。
- [x] 算出した「総売上高」「総仕入高」を、期計パネル、年間合計表、および【印刷・PDF出力プレビュー画面】のすべてに明示的に反映させる。

### [x] 要件4: 全体的な「分かりやすさ」のブラッシュアップ（UI/UX改善）
- [x] ユーザーの入力が必要なセルやフォームの背景色（薄い黄色/青など）やプレースホルダーを調整し、「入力箇所」を視覚的に強調する。
- [x] 各種ボタンのテキストにアイコンを組み合わせ（例：「📊 データ入力・確認」「💾 データを保存」など）、アクションの結果が直感的に伝わるようにする。
- [x] 画面全体のレイアウト（余白、文字サイズ、テーブルの境界線）を調整し、情報が整然と並び、初見でも迷わないUIに改善する。