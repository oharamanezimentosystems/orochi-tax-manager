# OROCHI System 法人化対応（決算月動的シフト）TODOリスト

## 🤖 Claude Codeへの指示
あなたはOROCHI Systemの機能拡張を担当するシニアフロントエンドエンジニアです。
現在、個人事業主前提（12月決算固定）で組まれているシステムを、法人の任意の「決算月」に合わせて各期の対象月が自動でシフトする構造に拡張します。

以下のルールに従って作業を進めてください。
1. **ステップバイステップで実行**: 一度にすべてを書き換えず、ステップを1つずつ実装・テストしてください。
2. **完了ごとのファイル更新**: サブタスクが1つ完了するごとに、この `todo0630.md` の `[ ]` を `[x]` に書き換えて保存してください。
3. **人間への確認**: 各ステップ（大項目）が完了するごとに、次のステップに進む前に必ず人間にレビューを求めてください。
4. **仕様の参照**: 基本的なデータ構造や全体の集計ルールは `space.md` を参照してください。

---

## 📋 タスクリスト

### [x] ステップ1: 顧問先設定モーダルへの「法人設定」の追加
- [x] `app/dashboard/page.tsx` の「顧問先設定モーダル（`isSettingsOpen`）」に、個人/法人の切り替えUI（ラジオボタンまたはチェックボックス）を追加する。
- [x] 「法人」が選択された場合のみ、決算月を「1月〜12月」から選択できるセレクトボックス（`<select>`）を表示させる。
- [x] 保存処理（`saveSettings`）を修正し、Firestoreのドキュメントに `isCorporate` (boolean) と `closingMonth` (number) が正しく保存・更新されるようにする。

### [x] ステップ2: ダッシュボード（一覧画面）の期間表記の動的化
- [x] `app/dashboard/page.tsx` のマトリクステーブルのヘッダーにある固定文字「第1期(1月～5月)」などの表記を、顧問先の決算月（`closingMonth`）に合わせて動的に計算して表示するロジック（関数）を実装する。
  - 計算ルール（5ヶ月・4ヶ月・3ヶ月の割り振り）:
    - 第3期: 決算月、およびその直前2ヶ月 (例: 5月決算なら 3, 4, 5月)
    - 第2期: 第3期の直前4ヶ月 (例: 5月決算なら 11, 12, 1, 2月)
    - 第1期: 残りの期首から5ヶ月 (例: 5月決算なら 6, 7, 8, 9, 10月)
- [x] 個人事業主（または未設定）の場合は、従来通りの期間（1〜5月、6〜9月、10〜12月）が表示されるよう、完全な後方互換性を維持する。

### [x] ステップ3: 詳細画面（タスク管理）の対象月動的算出の実装
- [x] `app/dashboard/detail/page.tsx` 内のハードコードされた `TERM_MONTHS` 定数を廃止し、顧問先の `closingMonth` に応じて対象月の配列を動的に返す関数 `getTermMonths(closingMonth, term)` を実装する。
- [x] 画面内の `currentMonths` や売上・仕入入力テーブル、突合確認表の月次ループ処理が、この動的な月配列を正しく読み込んでレンダリングできるように修正する。

### [x] ステップ4: 印刷・PDF出力画面への法人対応の連動
- [x] 印刷プレビュー表示モード（`printTarget` がオンの時）のテーブル見出しや、期計・年間合計表の集計ループ処理も、動的に算出された月配列と決算月ルールに完全に連動させる。
- [x] 法人の場合の「対象年度」の表記（例：5月決算法人の場合、2025年6月〜2026年5月をどう年度表記するか等）が崩れないか確認し、適切に調整する。